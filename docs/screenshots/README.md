# Screenshots

UI screenshots of the dashboard, referenced from the main README.

Suggested files:

- `dashboard.png` — pass rates by category across runs
- `run_detail.png` — a single run's results
- `failure_detail.png` — one failing case: prompt, target response, judge rationale

Use PNG, keep each under ~1 MB, and use lowercase-hyphenated names.
